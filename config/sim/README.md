# sim 層の設定

シミュレーションエンジン共通の設定（ゲームタイトルに依存しない部分）。

- `engine.json` — エンジン既定値
- `object_types/SCHEMA.md` — オブジェクト型の capabilities スキーマ（JSON の置き場所は `config/game/object_types/`）

ゲーム固有の種族・ワールド・具体的な object_types（`rock`, `poison_fog` など）は `config/game/` を参照。
